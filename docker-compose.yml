services:
  postgres:
    image: postgres:16
    container_name: scribeloop-postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: scribeloop
      POSTGRES_USER: scribeloop
      POSTGRES_PASSWORD: scribeloop
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
